# 更新紀錄 — AA 創作翻譯輔助小工具

以版號為單位彙整每次更新的摘要。最新版本置於最上方。
逐筆技術細節請見 git commit 歷史；本檔僅記人類可讀的更新重點。

## v1.80

- 「📂 檔案列表」浮層改用 `Qt.Popup` 旗標：點擊面板以外任何位置時自動關閉（Qt 內建行為），不再需要按 Esc 或「✕」也能收起。

## v1.79

- 「📂 檔案列表」浮層調整：深灰底（`#343a40`）配深色清單背景，與工具列風格一致；右上角加入「✕」關閉鈕；改為列出資料夾中**全部** HTML 檔（不再只取前後 10 筆），目前檔案仍置中、可上下捲動。
- 編輯器工具列：移除「📕 用語集」按鈕（Alt+5 快捷鍵與「用語集」側面板本身保留可用），改在「對話框(全)」右側放置「📂 檔案列表」按鈕，點擊開啟主畫面的檔案列表浮層。

## v1.78

- 首頁右上工具列新增「📂 檔案列表」按鈕（位於 Wiki 對照左側），點擊開合浮層面板：列出「最後開啟過的檔案」所在資料夾的相鄰 HTML，依檔名排序、目前檔案置於中段、上下各 10 個。檔名右對齊、從左側裁切，確保話數與副檔名永遠看得到；雙擊任一項即以該檔重新進入編輯面板（會還原暫存原文／提取／翻譯）。記住的檔案路徑持久化於 `aa_settings_cache.json` 的 `last_opened_file`，重啟後仍可直接列出。

## v1.77

- 設定新增「提取日文時：跳過與術語原文完全相同的字串」開關（持久化於 `aa_settings_cache.json` 的 `glossary_skip_extract`，預設關閉）。開啟後，提取結束時若某條結果與術語表（一般＋臨時）中某條術語的原文一字不差完全相同，則從提取結果中剔除——避免把已會被全文替換掉的術語再列出來請 AI 翻譯。影響主畫面提取、自動翻譯提取、以及單字假名提取三條路徑；比對對象包含 kana fold 變體。

## v1.76

- 對話框對齊：**新增「漢字框」（kanji_box）支援**。AA 中常見以漢字當邊框的對話框 —— 上框 `ミ川+彡`、下框 `彡川+ミ`（上下角落 ミ／彡 互換）、內容列 `三…三`（如「ミ川川川 彡」/「三　…　三」/「彡川川川 ミ」）。先前歸類為「特殊框（不支援）」、會 fallback 成普通框只半對齊或回傳 ⚠️ 錯誤。現在 `adjust_bubble()` 與 `adjust_all_bubbles()` 都會正確偵測並對齊；左側帶臉部 AA 前綴的變體也支援（框體角落到角落等寬，符合 AA 慣例的左圖右框排版）。
- 提取演算法 failcase 維護：修正 latin 前綴 AA 碎片誤抓。
  - 像「rへへハ」這種 latin 字母開頭 + 重複同助詞 + 片假名的 AA 碎片，因「count-based 多助詞 +2 bonus」對「同一助詞重複（へへ）」也加分，配合 `_AA_LATIN_PREFIX_RE` 既有 -3 不夠擋而被誤抓。現在 `strong_jp`（密度豁免）改用「相異助詞數」(distinct)，且 `_AA_LATIN_PREFIX_RE` 懲罰從 -3 提升至 -6（base 中唯一命中此規則的「Anリュウシ…」走拉長念法 bypass 評分，不受影響）。`まだだ` 這類同字助詞但合法的日文片段不受影響（由 count-based bonus 保留通過）。

## v1.75

- 修正：`extract_work_title` 對「`｜` 全形管線開頭、與前文黏在一起」的站名後綴比對不到的問題（例：`…２｜やる夫ＡＧＥ`）。原因是 `_SITE_SUFFIX_RE` 要求至少一個前置空白。獨立出一條 `_SITE_PIPE_SUFFIX_RE`（管線本身即分隔符，前置空白可有可無），對 `|`／`｜` 開頭的站名適用；一般站名仍走原本的「需前置空白」規則，避免誤吃像「日々」結尾的合法標題。`｜やる夫ＡＧＥ` 等管線站名現在不論黏字或有空白都會被去除。

## v1.74

- 修正：自動翻譯結束時面板 Log **重複出現兩份總結**（一份「──────── 自動翻譯總結 ────────」+ 一份「──────── 總結 ────────」）。原因是 coordinator 的 `_print_summary` 透過 `progress` callback 寫進面板 Log，之後 GUI 的 `_auto_translate_done` 又印一份更完整的總結。`run_auto_translate` 新增 `print_summary` 參數（CLI 預設 True 不變），GUI 端傳 False 抑制 coordinator 那份，面板 Log 只留 GUI 版（內含 next_url／模型不符等較完整資訊）。

## v1.73

- 網址讀取記錄的「標題快速篩選按鈕」（搜尋框右側、以及展開面板）不再顯示被 `_SITE_NAMES` 清單過濾的站名。標題正規化現會先套用與「自動填入作品名稱」相同的站名過濾（共用同一份維護清單），因此像「安価でやるお！」「| やる夫広場」這類非破折號／非泛用標籤的站名也會被去除；新增要過濾的站名仍只需維護 `_SITE_NAMES` 一處。

## v1.72

- 修正自動翻譯「連線設定」浮層的「💾 儲存連線設定」按下去沒反應的問題（按鈕內部參照到已不存在的 `backend_combo`，按下時例外中斷；改用目前主頁選定的後端）。
- 設定面板底部按鈕順序對調，改為「確定」在左、「取消」在右。
- 「設定」浮層與自動翻譯「連線設定」浮層在打開時，按 Esc 即可關閉（打開時自動把焦點移入浮層，故一開啟就能按 Esc）。

## v1.71

- 設定改以「浮層面板」開合呈現，不再開獨立的設定視窗。按右上 ⚙ 設定鈕會在當前頁面上方疊出設定面板（比照自動翻譯的「連線設定」浮層），再按一次、或按面板內「取消／確定」即收起；視窗縮放時面板會自動重新定位。
- 設定面板新增選項「編輯器全文替換存入術語時：同步加入批次搜尋『快速替換』」（預設關閉、跨次開啟記住）。勾選後，在編輯器「全文替換」勾選「存入術語」執行替換時，該術語除了存入主術語表，也會同步加到「批次搜尋」面板右上「快速替換」清單最上面（重複略過；批次搜尋尚未開啟時略過同步）。

## v1.70

- 設定視窗新增選項「編輯器內複製時：自動填入『全文替換』原文框」（預設關閉、跨次開啟記住）。勾選後，在編輯器內按複製（Ctrl+C 或右鍵選單「複製」）時，會把複製的內容自動填入工具列「全文替換」的原文框；原文框若已有內容會先清空再填入。編輯模式與「所見即所得」模式皆生效。

## v1.69

- 對話框對齊 failcase 維護：方框「右側轉角前有空格」修正。像「┌──　┐」這種右上/右下轉角前留有空白（含 hair space U+200A）的方框，原本因 `_BOX_*_RE` 要求橫線緊接轉角而偵測不到，會 fallback 成普通框、只對齊內容列、上下角落沒對齊。現在偵測容忍轉角前空白（`([─━]+)\s*([┐┓])`），這類方框會被正確判為方框並完整對齊。
- 提取演算法 failcase 維護：修兩類誤抓。
  - AA 雙豎線夾字：像「||しし||」這種夾在 AA 雙豎線 `||` 間的兩字重複平假名，被誤判為「對話框內對話」而抽出。現在「同字雙豎線」（`||`／`｜｜`）不算乾淨的對話框邊框；但「不同豎線字相鄰」（如 AA 牆 `|` 緊鄰 box 邊框 `┃`，「|┃その時┃」）仍正常。
  - 同字重複的拉長型：像「ソ　ソ　ソ　ソ」這種同一字以空格拉開重複的 AA 裝飾，被「拉長念法」偵測誤抓。現在拉長念法要求非標點字元相異度 ≥ 2（真實拉長念法如「シ　ロ　ウ　殿」字元有變化）。

## v1.68

- 自動翻譯「自然跑完設定話數」時，也會把下一話的接續網址自動帶回「起始網址」欄（先前只有中途停止／暫停／中止才會帶）。例如設定 5 話、順利翻完 5 話後，起始網址會更新成第 6 話，直接按「開始」即可接續下一批。若已翻到整篇最後一話（沒有下一話）則不帶。

## v1.67

- 提取演算法 failcase 維護：修好兩個漏抓。
  - 被 AA 裝飾夾住的對話漏抓：像「三　なるほどなー　三」這種被裝飾字元（三 等）左右夾住的對話，因局部 AA 密度達 1.0 被密度懲罰剔除。新增「≥5 連續真平假名 run」為強對話信號（AA 圖幾乎湊不出 5 個連續真平假名），讓這類對話豁免密度懲罰；同字重複（如「あああああ」）仍由既有規則擋下。
  - 說話者名牌前綴未去除：像「【カズマ】おのれ…」會把說話者名牌一起抽進譯文。現在提取結果開頭的「【說話者】」短名牌（不含句末標點／冒號、且後面有對話）會被剝除，只留對話「おのれ…」。內容型【…】（如「【流石に酷過ぎない？】：…」含 `？`）不受影響。

## v1.66

- 術語表「避免套用到 AA 圖」**規則 B（緊鄰片假名）修正連排術語誤擋**。先前像「ハクタイジム」這種兩個相鄰術語（`ハクタイ`＋`ジム`）連排時，逐一判定會各自把對方看成「緊鄰片假名碎片」而雙雙不套用。現在會先蒐集整行所有術語命中範圍，相鄰片假名若也在同輪術語覆蓋範圍內就不算碎片——兩者合起來完整覆蓋整段片假名詞，正常套用。真碎片（如「レオリオ」中的「リオ」，左鄰未被覆蓋）仍照常保留原文。
- 編輯器 **Tab 鍵改為插入 4 個半形空白**（取代預設的 tab 字元），避免 AA 圖排版混入 tab 造成對齊跑掉。編輯模式與 WYSIWYG（Alt+3）皆生效，唯讀的比對檢視不受影響。

## v1.65

- 提取演算法 failcase 維護：修好三類 AA 碎片誤抓。
  - 純片假名碎片（如「ニニァ-」：片假名＋小字＋標點、無平假名）：「無平假名候選」懲罰門檻由 len ≥ 5 放寬到 ≥ 4（含全形 `！` 等強信號者仍豁免，如「（クソ！」不受影響）。
  - 高密度混合雜湊（如「にう--ｒぅ.」：局部 AA 密度達 1.0、含 `--` 與全形 latin）：移除過弱的 `has_run 且長度 ≥ 7` strong_jp 豁免——這類長碎片不該因「含一段假名」就免疫密度／跳轉率懲罰；真實長對話幾乎都另含 ≥2 助詞或 `！？。`，已由其他條件涵蓋。
  - 結尾孤立 AA 筆畫片假名（如「弋でりノ」結尾的「ノ」）：邊界擴展會把結尾的孤立 AA 筆畫片假名（ノ／ミ／ヽ 等同屬 AA 裝飾符號者）一起吃進來；現在會剝除這類結尾字元，使候選回到真實邊界（縮短後多由短候選孤立過濾擋下）。

## v1.64

- 修正：在網址讀取面板選好網址、面板自動關閉返回時，現在會依「當初是從哪裡進來的」返回——從首頁進入就回首頁、從自動翻譯（🌐 網址記錄）進入就回自動翻譯。先前自動關閉與按 ESC 這兩條路徑會固定回首頁，沒有遵守來源頁（只有導覽列「返回」鈕有）。三條返回路徑（返回鈕／ESC／抓取成功自動關閉）已統一處理。

## v1.63

- 編輯器 **Alt+A（補／消空白）擴充支援 WYSIWYG（Alt+3）模式**。原本只能在編輯模式用，現在 WYSIWYG 預覽下也能直接補／消空白。為避免破壞上色：若選取範圍含上色標籤（非黑前景色），WYSIWYG 下會拒絕執行並提示「請避開上色文字」；不含上色的選取則正常執行（並自動維持行高）。比對模式（Alt+2）仍不可用。

## v1.62

- 編輯器新增快捷鍵 **Alt+A：補／消空白自動切換**。選取文字後按 Alt+A：若選取範圍含空白（半形或全形）→ 執行「消空白」；若不含空白 → 執行「補空白」（每字間插入全形空白，數量沿用工具列設定）。與工具列「消空白／補空白」按鈕一致，僅在純編輯模式作用（比對／WYSIWYG 模式會提示先回編輯模式）。

## v1.61

- 自動翻譯導覽列調整：
  - 「自動翻譯」標題與「⚙ 連線設定」按鈕位置對調（標題在左、連線設定在右）。
  - 連線設定右側新增「🌐 網址記錄」按鈕：點了會切到網址讀取面板，可在那邊直接挑/換網址；按「返回」會回到自動翻譯（而非首頁），並把選定的網址帶回「起始網址」。
- 自動翻譯主頁的「翻譯方式」由下拉選單改為「瀏覽器／API」兩顆切換按鈕，被選中的會以藍底顏色提示。

## v1.60

- 🧪 實驗性「偵測右側 AA 圖並補空白」新增**純假名文字排除**：被替換文字右側的殘留內容若整段都是假名（平假名／片假名／半形片假名，可夾雜括號），視為擬聲詞等文字（如「（ﾅﾃﾞﾅﾃﾞ」「（ﾆﾔﾘ）」）而非 AA 圖，不再誤補空白。即使其中含同被當 AA 裝飾的半形片假名（`ﾘ`/`ﾊ`/`ｿ` 等），只要右側全為假名即視為文字。

## v1.59

- 作品名稱標題清洗（`extract_work_title`）強化：
  - 修正 `_SITE_NAMES` 漏逗號 bug，導致 `【安価】` 等標籤被誤黏成單一字串而失效。
  - 修正 `| やる夫広場` 站名因開頭多一個空格而比對不到的問題（改為比照 `| 隣のAA` 不留開頭空白）。
  - 新增「標籤過濾」：`【...】` 形式的已知標籤（如 `【安価】`、`【R-18】`、`【あんこ】`）出現在標題**任何位置**（含中間）都會被去除，一般站名仍只去前綴／後綴。新增標籤直接加入 `_SITE_NAMES` 即可。
- 用語集「新加入條目置頂」：以各種自動方式（編輯器「存入術語」、批次搜尋「加入術語」）加入一般術語表的新條目，改為顯示在**最上面**而非最下面（手動編輯不受影響）。差異合併儲存（`merge_glossary_diff`）同步調整，重開程式 / 儲存設定後仍保留在最上面。

## v1.58

- API 連線模式的每日配額（RPD）冷卻改良：
  - 改用實際重置時間計算冷卻長度（每日 GMT+8 下午 3 點重置），Log 不再顯示倒數秒數，而是直接提示「額度將於 MM/DD 15:00（GMT+8）重置」。
  - 該金鑰的重置時間會持久化保存（以金鑰指紋為索引，不存明文金鑰，檔案不上傳）。下次開啟程式跑自動翻譯時：若已過重置時間 → 自動視為可用（重算）；若尚未到 → 沿用剩餘冷卻，不重複試探。

## v1.57

- API 連線模式使用 `gemini-3.5-flash` 時，預設帶入 thinking level = high（對應官方 `ThinkingConfig(thinking_level="high")`，REST 為 `generationConfig.thinkingConfig.thinkingLevel`）。其他模型不受影響。

## v1.56

- 提取演算法 failcase 維護：修好三個案例。
  - 漏抓修正：右端對話前若被「窄空白」（thin space／nbsp 等）隔開，原本會中斷「左側連續空白」判定使該對話漏抓（如「…　　　　　　 あ、みっけ」）。`_is_right_edge_dialogue` 的空白判定改用 `str.isspace()`，涵蓋半形／全形／窄空白。
  - 誤抓修正一：AA 圖中「單一平假名夾在兩片假名間」的碎片（如「モもト」）會被誤抓。新增 `_KATA_HIRA_KATA_RE` 懲罰，與既有「平假名夾片假名」(`_HIRA_KATA_HIRA_RE`) 對稱。
  - 誤抓修正二：6 字以上混合腳本雜湊（如「とつ竓芋ミz」：平假名＋罕用漢字＋片假名＋拉丁字母）跳轉率恰 0.5 時逃過懲罰。類別跳轉率門檻由 `> 0.5` 收緊為 `≥ 0.5`。

## v1.55

- 自動翻譯停止／暫停／中止（手動停止、撞額度暫停、模型不符、或抓取失敗無法續抓）時，會把「最後一次嘗試但未完成的那一話網址」自動回填到面板的「起始網址」欄，之後直接按「開始」即可從該話接續。

## v1.54

- 自動翻譯「連線設定」浮層版面微調：在「每 N 次送出後換新對話」與「API 模型」之間加入一條分隔線（區隔通用／瀏覽器設定與 API 專屬設定）；原本獨佔一列的「金鑰加密與儲存說明」改為「API 金鑰」欄位的浮動提示（滑鼠移過顯示），讓版面更精簡。

## v1.53

- 修正：提取後，由「單字特別提取」（額外單字提取規則）產生的項目原本一律排在輸出最底下；現在改為依 ID（來源行號）排序，這些項目會插回正確的行號位置。同一行內主提取項目仍排在額外單字之前。
- 主畫面提取與自動翻譯流程皆套用（兩條路徑共用同一輸出排版函式）。

## v1.52

- API 連線模式遇到伺服器暫時性錯誤（HTTP 5xx，例如 503「This model is currently experiencing high demand」高負載）時，改為**等待 2 分鐘後自動重試**，最多重試 5 次；連續失敗才跳過該話。這類錯誤不算金鑰問題，不會把金鑰標記為冷卻。等待期間可按「停止」中止。

## v1.51

- 修正：實驗性提取演算法的「逐步分析報告」(`analyze_extraction`) 與實際提取 (`extract_text`) 邏輯分歧，會誤導除錯。舊版報告只做「長度<3 剔除」、缺少拉長念法／全形片假名整句、allow_short 短句放行、短純片假名括號閘、短候選孤立過濾、括號補完等步驟。現在報告完整鏡射實際管線，且「倖存清單／最終結果」直接取自與 `extract_text` 相同的內部函式，保證報告結論與實際提取一致（已用 base.txt 全行逐行驗證 0 分歧）。

## v1.50

- 修正：術語表「避免套用到 AA 圖」（實驗性 avoid_aa）會把緊鄰連續裝飾標點的片假名碎片誤替換。例如 AA 圖中 `;;;;;;ノイ:::` 的 `ノイ`，因半形分號 `;` 不在 AA 噪聲定義內、連續重複時把周圍密度稀釋到門檻以下而漏判。新增規則：命中左/右緊鄰 ≥3 個相同裝飾標點（分號、底線等，排除省略號/破折號/句讀等正當重複標點）即視為 AA 圖、保留原文不替換。`でも`/`のに` 等正常替換不受影響。
- 修正：實驗性日文提取演算法會把 AA 圖中「整段都是助詞、無實詞」的兩字平假名碎片誤抓。例如 `r､にへﾉ∧` 中的 `にへ`（に＋へ皆為助詞）。原因是「多重助詞＝強對話信號」會給分數加成並豁免局部 AA 密度懲罰；但多助詞信號需附著實詞才成立。現在整段都是助詞的短候選不再享加成與密度豁免，在高 AA 密度區會被剔除。`でも`、含實詞的多助詞片段（如「夢では」）等合法情況不受影響。

## v1.49

- API 連線模式的額度處理改良：
  - 某把金鑰撞到額度（429）時，會為它設一段「冷卻時間」並改用其餘金鑰繼續輪換；冷卻中的金鑰會被跳過，不再每次都重複試探已用完的金鑰。
  - 會偵測是「每分鐘速率上限(RPM)」還是「每日配額(RPD)」：RPM 採短冷卻（優先採用伺服器建議的重試秒數，否則 60 秒）稍後自動恢復；RPD 採長冷卻（24 小時），本次執行不再嘗試該金鑰，避免反覆無效請求。
  - 當所有金鑰同時冷卻：若很快會恢復（多為 RPM）會短暫等待後重試；若是每日配額或反覆全滿，則終止自動翻譯（已翻完的話皆已存檔，並印出可接續的網址）。
  - 等待期間可被「停止」按鈕中止。

## v1.48

- 「翻譯方式」（瀏覽器／API）選單從連線設定移回自動翻譯主頁，放在「輸出資料夾」下方、「開始自動翻譯」按鈕上方，方便常用切換。按「開始」時即以主頁的選擇為準（不必先按「儲存連線設定」）。
- API 模式下「Gem 網址」不再是必填（僅瀏覽器模式需要）。
- 連線設定浮層加寬約 10%（上限 600→660px）。
- 連線設定浮層高度改為依內容收緊，消除「儲存連線設定」按鈕下方過大的空白；內容超過視窗高度時才捲動。

## v1.47

- 自動翻譯面板改版：「Gemini Gem 網址／要求模型／每 N 次送出後換新對話」三個欄位從主頁移到「連線設定」裡，主頁只留起始網址、話數、作品名稱、檔名預覽、輸出資料夾。
- 「⚙ 連線設定」按鈕移到左上角（「← 返回首頁」的右側，僅自動翻譯頁顯示）。
- 連線設定的開啟方式改成浮層面板（比照網址讀取頁的「展開標題按鈕」），點按鈕在面板上展開、再按或按 ESC／「← 關閉」即收合，不再是整頁切換。
- 連線設定的「💾 儲存連線設定」現在會一併把移入的 Gem 網址／要求模型／換新對話次數一起存檔。

## v1.46

- 修正：套用術語表時，AI 譯文裡保留的片假名人名會被術語碎片硬切。例如譯文保留 `レオリオ`、術語表有「リオ=莉央」時，舊版會替換成 `レオ莉央`（把人名切開）。現在只要術語原文含片假名、且命中處左右仍緊鄰片假名，就判定為「更長片假名詞的碎片」而保留原文不替換（後接片假名敬稱如 サン/チャン 等仍正常替換）。
- 此保護一律啟用、不需開啟實驗性「避免套用到 AA 圖」選項；正常獨立的術語（如 クラピカ）替換不受影響。

## v1.45

- 新增設定「存入術語後自動儲存到設定檔」（預設關閉）。開啟後，凡是「存入術語」（編輯器全文替換勾選『存入術語』、批次搜尋『加入術語』等）把術語加進主術語表時，會自動把一般術語表永久寫入 `AA_Settings.json`（依「儲存設定時僅合併差異」設定決定合併或覆蓋），不必再另外按「儲存設定／儲存用語」。
- 關閉時（預設）維持原行為：只更新主畫面術語表與暫存，正式設定檔需手動儲存。

## v1.44

- 新增設定「套用術語表時：平假名／片假名互通」（預設關閉）。開啟後，術語原文（等號左側）的平假名與片假名視為相通，例如術語「ライザ=萊莎」會同時等同「らいざ=萊莎」（反之亦然），無論原文寫成哪種假名都能命中同一替換。
- 變體只在不與既有明確術語條目衝突時自動加入（明確寫的條目優先）；漢字、長音符號 ー、英數等非假名字元維持原樣。影響所有術語套用路徑（替換翻譯、重套術語、Alt+E 選取替換、自動翻譯等）。

## v1.43

- 新增：編輯器「📕 用語集」面板（Alt+5）。可在編輯器內直接編輯一般術語表，與主畫面雙向即時同步；有搜尋框、可調整寬度並持久化（與局部重套用面板一樣是右側 1/3 面板）。
- 「💾 儲存用語」按鈕只把術語寫入設定檔（不動 filter／正則／其他設定）；左鍵依「僅儲存差異」設定、右鍵強制覆蓋，與主畫面「儲存設定」行為一致。

## v1.42

- 未翻譯防呆強化：偵測到「只把原文吐回」時，重試前會**先開一個新對話**再重送（同一對話重送極可能吐出一模一樣的結果）。瀏覽器後端＝重開 Gem 對話並重新確認登入/模型；API 後端因每次請求獨立故為 no-op。

## v1.41

- 自動翻譯模型處理大改：模型不符時改為**自動從模型選單切換**到要求的模型，不再需要使用者手動選。
- 若要求的模型「額度已滿」（選單顯示「用量額度將於…」），改為**長時間等待額度恢復**（每 10 分鐘重試自動切換，最長 12 小時），恢復後自動切回繼續，而不是固定等 5 分鐘就放棄。
- 只有在自動切換失敗（選單選擇器失效）時，才退回原本「等使用者手動切換 5 分鐘」的行為。
- 全程可按「■ 停止」立即中止。

## v1.40

- 修正「標示翻譯完成卻沒實際翻譯」的問題，兩道防線：
  - 瀏覽器後端在生成判定完成後，多等約 3 秒才讀取回覆，避免串流尾端／DOM 還沒 render 完就讀走半截或舊內容。
  - 防呆：若回覆與原文以 ID 對齊比對有九成以上完全相同（疑似 Gemini 只把原文吐回、沒翻譯），會自動重試一次；仍相同則跳過該話、不存檔，並在總結列出。

## v1.39

- 自動翻譯時讀過的每個網址都會寫入「網址讀取紀錄」（含標題、投稿指紋、關聯連結），與手動讀取一致；主程式的檔案監看會自動把新紀錄刷新到 UI。

## v1.38

- 修正：首頁「⚙ 設定」打不開的問題（v1.29 重構移除了 `_orig_cache_path()`，但設定對話框仍呼叫它而拋例外）。
- 修正：自動翻譯面板按 ESC 沒反應；現在 ESC 在連線設定分頁會退回主分頁，在主分頁會返回首頁（子欄位有焦點時也有效）。

## v1.37

- 連線設定新增「瀏覽器模式使用 Gem（不發送 Prompt）」勾選框（預設勾選）：
  - 勾選 → 瀏覽器模式靠 Gem 內建人設，不送出翻譯 Prompt（給用 Gem 的使用者）
  - 取消勾選 → 瀏覽器模式也會送 Prompt（給不用 Gem 的使用者）
  - API 模式一律送 Prompt，不受此選項影響

## v1.36

- 連線設定的「翻譯 Prompt」文字框改為兩種模式都可用、也都會送出：
  - API 模式 → 當作系統指令
  - 瀏覽器模式 → 附加在每個新對話的第一則訊息開頭（用內建人設的 Gem 可留空）
- 原本此欄只在 API 模式可編輯且只套用於 API，現在瀏覽器模式也能放 prompt。

## v1.35

- 自動翻譯新增「⚙ 連線設定」分頁，可切換翻譯方式：
  - **現行**（瀏覽器操控網頁版 Gemini）
  - **Google API**（直接呼叫 Gemini API）
- API 模式支援多把金鑰：每行一把，送請求時 round-robin 輪換；遇 429 自動換下一把重試。
- API 模式可選模型：gemini-2.5-pro / gemini-3.1-pro / gemini-3-flash / gemini-3.5-flash / gemini-3.1-flash-lite，並可填系統指令（翻譯人設）。
- 金鑰以 **Windows DPAPI 加密**存於 `aa_api_keys.dat`（綁定本機帳號、不需主密碼、已列入 .gitignore，不會上傳）。
- 後端／模型／系統指令持久化於設定；金鑰另存於上述加密檔。

## v1.34

- 自動翻譯「檔名預覽」可顯示實際檔名：新增「🔄 試算」鈕，讀取起始網址、解析後顯示這一話真正會寫入的檔名（含偵測到的話數與同名序號）。
- 開啟面板時若起始網址已在本地快取會自動帶出實際檔名（不上網、不卡 UI）；未快取則顯示靜態模板，按「🔄 試算」才實際抓取。

## v1.33

- 自動翻譯檔名衝突序號改用 dash：`Title_34.html` / `Title_34-2.html` / `Title_34-3.html`（原本為 `_2`、`_3`），與作品中常見的「34、34-2」並列習慣一致。

## v1.32

- 自動翻譯面板「檔名預覽」欄位底色由近白 `#f1f3f5` 改為明確的中灰 `#d6d8db`（含 `#adb5bd` 邊框），與其他可編輯欄位的白底有明顯視覺區隔。
- 還原 v1.31：編輯器（editor / orig_view / preview_view）的預設底色恢復為 `#ffffff`，因實際需求只是要改自動翻譯面板的預覽欄。`EDITOR_DEFAULT_BG` 常數與 `editor_bg_migrated` 遷移欄位一併移除。

## v1.31

- （已撤回，見 v1.32）編輯器預設底色改為偏暖米灰；實作與當時的需求對焦錯誤。

## v1.30

- 「🔎 重找原文」鈕加上後備路徑：暫存查不到時，若 `url_history` 有指紋相符的網址，會直接抓網頁、解析、重建原文並寫回暫存。讓 v1.28 以前的舊自動翻譯檔也能一鍵對齊原文。

## v1.29

- 修正：自動翻譯不會寫入 `aa_original_cache.json`，導致編輯器原文模式對不上的問題。每話現在都會以投稿指紋（日期 + 時間 + 作者 ID）為索引同步存入原文暫存。
- 編輯器工具列新增「🔎 重找原文」鈕：依當前 HTML 的投稿指紋重查原文暫存，立刻更新原文視圖，用於 cache 失同步時手動重新對齊。
- 重構：原文暫存 I/O 抽出至 `aa_tool/original_cache.py`，與手動／自動流程共用；`MainWindow` 的 `save_original_for_file` / `load_original_for_file` 改為轉接層。

## v1.28

- 模型不符時改為「暫停等待」而非立即中止：偵測到模型與要求不符會在 Log 顯示提示，並每 30 秒提醒一次，等使用者在瀏覽器手動切換到正確模型即繼續；最多等 5 分鐘，逾時才真正中止。等待中按下「■ 停止」可立即中止。
- 自動翻譯面板新增「作品名稱」與「檔名預覽」欄位，行為與首頁一致：
  - 設定「自動填入作品名稱」開啟時 → 檔名由每話 page_title 自動萃取，作品名稱欄停用；
  - 關閉時 → 檔名為 `{作品名稱}_{偵測到的話數}.html`，作品名稱欄與首頁同步；
  - 任一模式下同名檔案自動加 `_2`、`_3` 序號。
- CLI 加 `--doc-title` 與 `--auto-fill-title` 旗標。

## v1.27

- 自動翻譯面板新增「要求模型」下拉選單：Pro／Flash／Flash-Lite／不檢查。
- 翻譯啟動及每次新對話開啟時，若偵測到的模型與要求不符，整批自動中止並彈出警告。
- 讀不到模型字串時不會阻擋（Gemini 改版若讀不到指示元素，不會把使用者整批鎖死），改以警告 Log 提示。
- 設定持久化於 `gemini_required_model`，預設 `pro`。CLI 加 `--required-model`。

## v1.26

- 自動翻譯改為「分頁」模式：原本的對話框拆成一個內嵌面板（stack index 4）。
- 「自動翻譯」按鈕從工具列右側移到「編輯模式」右邊，樣式與「批次搜尋」一致。
- 面板上半為設定欄位、下半為即時 Log 文字框（黑底亮字、自動捲到底）。
- 設定多了「每 N 次送出後換新對話」欄位（對應 `gemini_max_per_session`，持久化）。
- 自動翻譯啟動與每次新對話開啟時，Log 會顯示「目前使用模型：xx」（Gemini 2025 改版後預設模型選項消失，這項用來確認是否為 Pro；讀不到時顯示警告）。

## v1.25

- 新增：實驗性選項「套用術語表時避免套用到 AA 圖」。開啟後全域套用術語表時會略過疑似落在 AA 圖上的命中（依周圍 AA 噪聲密度、緊鄰片假名判定），避免術語表把 AA 圖中的片假名碎片誤替換。
- 修正：右側 AA 補空白改以「顯示寬度」計算（半形=1/全形=2），解決半形空白／半形假名造成的累積誤差、AA 圖填得歪掉的問題。
- 調整：「🔧提取Debug」改為對話框內提供輸入框，貼上文字後在框內選取要分析的片段，不再讀取主畫面選取。
- 自動翻譯：「⚡ 自動翻譯」按鈕移到主畫面工具列（Wiki 對照左側）。
- 自動翻譯：執行中於頁面頂部顯示常駐進度橫幅，含「■ 停止」鈕可隨時中止。
- 自動翻譯：新增「翻譯到最後一話」選項，勾選後忽略話數一路翻到底。
- 自動翻譯：話數與「到最後一話」設定會持久化，下次打開對話框自動帶入上次值。
- 自動翻譯：修正提取的行號 ID 比手動流程少 2 的問題（原因：手動流程會在原文前面 prepend 作品標題 + 空行）。
- 自動翻譯：分段門檻由 80 行調整為 800 行（實務上短於這個長度都不需分段；切點固定在整行邊界，永遠不會把單一 ID 切開）。
- 自動翻譯：偵測 Gemini 卡住超過 10 分鐘無回應時，自動開新對話重試一次。
- 自動翻譯：偵測 Gemini 回覆疑似被審查（極短且非 ID|文 格式）時跳過該話，最後總結顯示。

## v1.24

- 新增：連續多話自動翻譯。「⚡ 自動翻譯」按鈕一次設定起始網址與話數，程式即自動完成「抓取 → 提取 → 翻譯 → 替換 → 存檔 → 下一話」整個流程。
- 翻譯交由 Playwright 操控網頁版 Gemini 的翻譯 Gem 完成（沿用既有 Gem，不需 API）。
- 同一對話 session 最多翻譯 3 次即自動開新對話，避免上下文過長拖低翻譯品質。
- 撞到 Gemini 額度上限時自動暫停並保留已完成進度，提示可接續的網址。
- 也提供 CLI：`python aa_auto_translate.py --url <網址> --count <話數> --out <資料夾>`。
- 需求：`pip install playwright` 後執行 `playwright install chromium`。

## v1.23

- 修正：同一行有多句被翻譯時，只有最後一段會補空白（原本每段都補，會在句子間塞入多餘空白）。
- 修正：對話框修正等操作後按 Ctrl+Z，不再需要按兩次（行高格式不再自成獨立的還原步驟）。
- 修正：編輯器「行高」欄位在 80~140% 區間調整無效的問題（CJK fallback 下限未隨行高%縮放）。

## v1.22

- 新增：編輯器 `Ctrl+F` 搜尋列加入「行高」欄位（80~300%），可手動調整行高並持久化。
- 修正：實驗性「右側 AA 補空白」功能不再把純橫線分隔線（如 `─── `）誤判為 AA 圖。

## v1.21

- 修正：套用翻譯時，原文若夾帶 Unicode 雙向控制字元（如 U+202C）導致整句無法替換的問題。
- 調整：「自動填入作品名稱」設定開啟時，自動填入的標題不再寫入網址歷史紀錄，僅使用者手填的標題會記錄。
- 調整：忽略留言且未填作者名稱時，自動偵測作者會略過名稱含「名無」的匿名留言者，往下一樓尋找真正的作者。
- 實驗性提取：支援圈號數字選項（①～⑳），「⑦～⑨出来る」這類合併多選項格式可完整提取。
- 實驗性提取：邊界擴展支援半形 `+` 與 `★☆`，含 `+` 的句子（如「+が付くのに必要な魔石」）不再被拆成兩句。
- 實驗性提取：新增「候選自身 AA 噪聲密度」過濾，剔除錨點假名混入點線雜訊的 AA 碎片（如「丈ｒうっ.o.,.」）。
- 實驗性提取：新增「【說話者】───」名牌前綴偵測，名牌後的對話不再因破折號被誤判為 AA 而漏抓。
- 實驗性提取：對話框邊框新增 `┃`；對話框內的短候選不再被孤立過濾剔除（如「┃その時┃」）。
- 實驗性提取：短句支援假名與標點間夾省略號（如「（え……？）」）；純平假名 run（如「まだだ」）列為強短信號。
- 實驗性提取：括號內的純片假名連續喊叫可逐段提取（如「（クソ！　クソ！　クソッタレッ！！）」）。
- 實驗性提取：行尾的漢字+句末標點短句（如「何？」）放寬到長度 2 可提取。
- 實驗性提取：新增「全形片假名整句」偵測，角色全程以片假名講話的對話（如「ホウシシュゾク　ハ　イシガ　ナイカギリ」）可整句保留空白提取。
- 修正：單字特別提取的句末標點收緊為全形 `。？！`，不再被 AA 圖的半形 `!`/`?` 裝飾（如「 ン!」）誤抓。
- 實驗性提取：邊界擴展支援半形單引號 `'`，作者把 `''` 當雙引號用的句子（如「な''気配''と''匂い''が」）不再被切斷。

## v1.20

- 實驗性日文提取演算法（錨點掃描＋分數制過濾）。
- 替換右側 AA 補空白、編輯器 Redo／複製網址、指紋原文暫存等。
- 使用說明書更新至 v1.20（韓文／實驗提取、補空白、Redo、複製網址、指紋暫存等）。

> v1.20 以前的更新請參閱 git commit 歷史。
