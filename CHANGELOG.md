# 更新紀錄 — AA 創作翻譯輔助小工具

以版號為單位彙整每次更新的摘要。最新版本置於最上方。
逐筆技術細節請見 git commit 歷史；本檔僅記人類可讀的更新重點。

## v1.28

- 模型不符時改為「暫停等待」而非立即中止：偵測到模型與要求不符會在 Log 顯示提示，並每 30 秒提醒一次，等使用者在瀏覽器手動切換到正確模型即繼續；最多等 5 分鐘，逾時才真正中止。等待中按下「■ 停止」可立即中止。
- 自動翻譯面板新增「作品名稱」與「檔名預覽」欄位，行為與首頁一致：
  - 設定「自動填入作品名稱」開啟時 → 檔名由每話 page_title 自動萃取，作品名稱欄停用；
  - 關閉時 → 檔名為 `{作品名稱}_{偵測到的話數}.html`，作品名稱欄與首頁同步；
  - 任一模式下同名檔案自動加 `_2`、`_3` 序號。
- CLI 加 `--doc-title` 與 `--auto-fill-title` 旗標。

## v1.27

- 自動翻譯面板新增「要求模型」下拉選單：Pro／Flash／Flash-Lite／不檢查。
- 翻譯啟動及每次新對話開啟時，若偵測到的模型與要求不符，整批自動中止並彈出警告。
- 讀不到模型字串時不會阻擋（Gemini 改版若讀不到指示元素，不會把使用者整批鎖死），改以警告 Log 提示。
- 設定持久化於 `gemini_required_model`，預設 `pro`。CLI 加 `--required-model`。

## v1.26

- 自動翻譯改為「分頁」模式：原本的對話框拆成一個內嵌面板（stack index 4）。
- 「自動翻譯」按鈕從工具列右側移到「編輯模式」右邊，樣式與「批次搜尋」一致。
- 面板上半為設定欄位、下半為即時 Log 文字框（黑底亮字、自動捲到底）。
- 設定多了「每 N 次送出後換新對話」欄位（對應 `gemini_max_per_session`，持久化）。
- 自動翻譯啟動與每次新對話開啟時，Log 會顯示「目前使用模型：xx」（Gemini 2025 改版後預設模型選項消失，這項用來確認是否為 Pro；讀不到時顯示警告）。

## v1.25

- 新增：實驗性選項「套用術語表時避免套用到 AA 圖」。開啟後全域套用術語表時會略過疑似落在 AA 圖上的命中（依周圍 AA 噪聲密度、緊鄰片假名判定），避免術語表把 AA 圖中的片假名碎片誤替換。
- 修正：右側 AA 補空白改以「顯示寬度」計算（半形=1/全形=2），解決半形空白／半形假名造成的累積誤差、AA 圖填得歪掉的問題。
- 調整：「🔧提取Debug」改為對話框內提供輸入框，貼上文字後在框內選取要分析的片段，不再讀取主畫面選取。
- 自動翻譯：「⚡ 自動翻譯」按鈕移到主畫面工具列（Wiki 對照左側）。
- 自動翻譯：執行中於頁面頂部顯示常駐進度橫幅，含「■ 停止」鈕可隨時中止。
- 自動翻譯：新增「翻譯到最後一話」選項，勾選後忽略話數一路翻到底。
- 自動翻譯：話數與「到最後一話」設定會持久化，下次打開對話框自動帶入上次值。
- 自動翻譯：修正提取的行號 ID 比手動流程少 2 的問題（原因：手動流程會在原文前面 prepend 作品標題 + 空行）。
- 自動翻譯：分段門檻由 80 行調整為 800 行（實務上短於這個長度都不需分段；切點固定在整行邊界，永遠不會把單一 ID 切開）。
- 自動翻譯：偵測 Gemini 卡住超過 10 分鐘無回應時，自動開新對話重試一次。
- 自動翻譯：偵測 Gemini 回覆疑似被審查（極短且非 ID|文 格式）時跳過該話，最後總結顯示。

## v1.24

- 新增：連續多話自動翻譯。「⚡ 自動翻譯」按鈕一次設定起始網址與話數，程式即自動完成「抓取 → 提取 → 翻譯 → 替換 → 存檔 → 下一話」整個流程。
- 翻譯交由 Playwright 操控網頁版 Gemini 的翻譯 Gem 完成（沿用既有 Gem，不需 API）。
- 同一對話 session 最多翻譯 3 次即自動開新對話，避免上下文過長拖低翻譯品質。
- 撞到 Gemini 額度上限時自動暫停並保留已完成進度，提示可接續的網址。
- 也提供 CLI：`python aa_auto_translate.py --url <網址> --count <話數> --out <資料夾>`。
- 需求：`pip install playwright` 後執行 `playwright install chromium`。

## v1.23

- 修正：同一行有多句被翻譯時，只有最後一段會補空白（原本每段都補，會在句子間塞入多餘空白）。
- 修正：對話框修正等操作後按 Ctrl+Z，不再需要按兩次（行高格式不再自成獨立的還原步驟）。
- 修正：編輯器「行高」欄位在 80~140% 區間調整無效的問題（CJK fallback 下限未隨行高%縮放）。

## v1.22

- 新增：編輯器 `Ctrl+F` 搜尋列加入「行高」欄位（80~300%），可手動調整行高並持久化。
- 修正：實驗性「右側 AA 補空白」功能不再把純橫線分隔線（如 `─── `）誤判為 AA 圖。

## v1.21

- 修正：套用翻譯時，原文若夾帶 Unicode 雙向控制字元（如 U+202C）導致整句無法替換的問題。
- 調整：「自動填入作品名稱」設定開啟時，自動填入的標題不再寫入網址歷史紀錄，僅使用者手填的標題會記錄。
- 調整：忽略留言且未填作者名稱時，自動偵測作者會略過名稱含「名無」的匿名留言者，往下一樓尋找真正的作者。
- 實驗性提取：支援圈號數字選項（①～⑳），「⑦～⑨出来る」這類合併多選項格式可完整提取。
- 實驗性提取：邊界擴展支援半形 `+` 與 `★☆`，含 `+` 的句子（如「+が付くのに必要な魔石」）不再被拆成兩句。
- 實驗性提取：新增「候選自身 AA 噪聲密度」過濾，剔除錨點假名混入點線雜訊的 AA 碎片（如「丈ｒうっ.o.,.」）。
- 實驗性提取：新增「【說話者】───」名牌前綴偵測，名牌後的對話不再因破折號被誤判為 AA 而漏抓。
- 實驗性提取：對話框邊框新增 `┃`；對話框內的短候選不再被孤立過濾剔除（如「┃その時┃」）。
- 實驗性提取：短句支援假名與標點間夾省略號（如「（え……？）」）；純平假名 run（如「まだだ」）列為強短信號。
- 實驗性提取：括號內的純片假名連續喊叫可逐段提取（如「（クソ！　クソ！　クソッタレッ！！）」）。

## v1.20

- 實驗性日文提取演算法（錨點掃描＋分數制過濾）。
- 替換右側 AA 補空白、編輯器 Redo／複製網址、指紋原文暫存等。
- 使用說明書更新至 v1.20（韓文／實驗提取、補空白、Redo、複製網址、指紋暫存等）。

> v1.20 以前的更新請參閱 git commit 歷史。
